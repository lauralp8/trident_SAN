# ================================================================================
# CONFIGURACIÓN TRIDENT SAN - Solo edita los valores necesarios
# ================================================================================
# 
# INSTRUCCIONES:
#   1. Edita solo los valores a la DERECHA de los dos puntos (:)
#   2. NO borres las líneas, solo cambia los valores
#   3. Los campos con # delante son opcionales (puedes ignorarlos)
#   4. Para usar un campo opcional, quita el # del inicio de la línea
#
# ================================================================================

# BACKEND - Configuración del almacenamiento NetApp
backend:
  # === CAMPOS OBLIGATORIOS ===
  managementLIF: sansvm-mgmt.demo.netapp.com
  dataLIF: sansvm.demo.netapp.com
  svm: sansvm
  credentials:
    name: trident-creds
  
  # === CAMPOS OPCIONALES - Metadatos y configuración básica ===
  # name: backend-jc-san
  # namespace: trident
  # version: 1
  # backendName: ""  # Si vacío, se genera automáticamente como: {storageDriverName}_{dataLIF}
  # storageDriverName: ontap-san
  # useREST: true
  # storagePrefix: trident
  
  # === PARÁMETROS CHAP ===
  # Habilita la autenticación CHAP bidireccional para iSCSI.
  # IMPORTANTE: Según la documentación de NetApp Trident, los valores CHAP
  # se definen DIRECTAMENTE aquí en el backend spec, NO en secret.yaml.
  useCHAP: true
  chapInitiatorSecret: "cl9qxWfnUFoinvk"
  chapTargetInitiatorSecret: "rwbwtuigONgbw"
  chapUserName: "uh2a1io325bFFILn"
  chapTargetUsername: "iJF4sgjrnwOwQ"
  
  # === OTROS PARÁMETROS ===
  # labels: ""
  # clientCertificate: ""
  # clientPrivateKey: ""
  # trustedCACertificate: ""
  # aggregate: ""
  # limitAggregateUsage: ""
  # limitVolumeSize: ""
  # limitVolumePoolSize: ""
  # lunsPerFlexvol: "100"
  # sanType: iscsi
  # formatOptions: ""
  # denyNewVolumePools: "false"
  
  # === DEBUG FLAGS ===
  # debugTraceFlags:
  #   api: false
  #   method: false
  
  # === DEFAULTS DE VOLÚMENES ===
  # defaults:
  #   spaceReserve: none
  #   spaceAllocation: "true"
  #   snapshotPolicy: none
  #   snapshotReserve: "0"
  #   encryption: "false"
  #   qosPolicy: ""
  #   adaptiveQosPolicy: ""
  #   tieringPolicy: none
  #   luksEncryption: ""
  #   nameTemplate: ""

# STORAGE CLASS - Nombre del storage class en Kubernetes/OpenShift
storageClass:
  # === CAMPOS OBLIGATORIOS ===
  name: rhoso-san
  
  # === CAMPOS OPCIONALES - Configuración del StorageClass ===
  # isDefault: true
  # syncWave: "5"
  # provisioner: csi.trident.netapp.io
  # reclaimPolicy: Delete  # Opciones: Delete, Retain
  # allowVolumeExpansion: true
  # volumeBindingMode: Immediate  # Opciones: Immediate, WaitForFirstConsumer
  
  # === PARÁMETROS DEL STORAGE CLASS ===
  # parameters:
  #   backendType: ontap-san
  #   fsType: ext4
  #   provisioningType: thin
  #   snapshots: "true"
# ================================================================================
# SECRET - Credenciales de acceso al SVM
# ================================================================================
# El secret de Kubernetes contiene SOLO las credenciales de acceso al SVM:
#   - username: Usuario administrador del SVM (ej: vsadmin)
#   - password: Contraseña del usuario
#
# NOTA: Los campos CHAP NO van en el secret, van directamente en el backend
# config (arriba) según la documentación oficial de NetApp Trident.
# El backend referencia el secret mediante backend.credentials.name
# ================================================================================